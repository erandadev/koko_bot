name: Scheduled API Request

on:
  schedule:
    # This runs every 15 minutes (Cron syntax)
    - cron: "*/15 * * * *"
  workflow_dispatch: # Allows you to run it manually for testing

jobs:
  send-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Run script
        env:
          KOKO_USERNAME: ${{ secrets.KOKO_USERNAME }}
          KOKO_PASSWORD: ${{ secrets.KOKO_PASSWORD }}
          MINTPAY_USERNAME: ${{ secrets.MINTPAY_USERNAME }}
          MINTPAY_PASSWORD: ${{ secrets.MINTPAY_PASSWORD }}
          DIRECTPAY_USERNAME: ${{ secrets.DIRECTPAY_USERNAME }}
          DIRECTPAY_PASSWORD: ${{ secrets.DIRECTPAY_PASSWORD }}
          DIRECTPAY_LNL_USERNAME: ${{ secrets.DIRECTPAY_LNL_USERNAME }}
          DIRECTPAY_LNL_PASSWORD: ${{ secrets.DIRECTPAY_LNL_PASSWORD }}
          DATA_SUBMIT_ENDPOIT: ${{ secrets.DATA_SUBMIT_ENDPOIT }}
        run: node scrape.js
